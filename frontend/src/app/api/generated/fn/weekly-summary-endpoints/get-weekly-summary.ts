/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WeeklySummaryResponse } from '../../models/weekly-summary-response';

export interface GetWeeklySummary$Params {
  id: number;
  weekStart?: string;
}

export function getWeeklySummary(http: HttpClient, rootUrl: string, params: GetWeeklySummary$Params, context?: HttpContext): Observable<StrictHttpResponse<WeeklySummaryResponse>> {
  const rb = new RequestBuilder(rootUrl, getWeeklySummary.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('weekStart', params.weekStart, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WeeklySummaryResponse>;
    })
  );
}

getWeeklySummary.PATH = '/api/accounts/{id}/weekly-summary';
