/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */


/**
 * A current value compared against its trailing baseline median.
 */
export interface MetricComparisonResponse {

  /**
   * Null means no baseline was computable at all (see the parent response's `dataStatus`). Zero
   * means a baseline exists and is genuinely zero — there is nothing to compare magnitude against, so
   * Status is Relay.Api.Features.WeeklySummary.ActivityStatus.NoVerdict in that case too. These two null
   * cases are intentionally distinguishable — do not collapse them.
   */
  baselineMedian?: number | null;
  current: number;

  /**
   * A fraction, not a percentage: 0.25 means +25%. Null whenever Relay.Api.Features.WeeklySummary.MetricComparisonResponse.Status is
   * Relay.Api.Features.WeeklySummary.ActivityStatus.NoVerdict.
   */
  deltaRatio?: number | null;
  status: 'below' | 'typical' | 'above' | 'noVerdict';
}
