/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { LocationSummaryResponse } from '../models/location-summary-response';
import { MetricComparisonResponse } from '../models/metric-comparison-response';
import { TypeBreakdownResponse } from '../models/type-breakdown-response';

/**
 * The classified weekly summary for one account and one week.
 */
export interface WeeklySummaryResponse {
  accountId: number;
  baselineWeeksUsed: number;
  byType: Array<TypeBreakdownResponse>;
  dataStatus: 'ok' | 'insufficientHistory' | 'noActivity';

  /**
   * Already sorted worst-first by the domain.
   */
  locations: Array<LocationSummaryResponse>;

  /**
   * The last local day included in the comparison window, as a human-facing display value —
   * `WeekEnd` for a completed week, or the anchor's local day for the in-progress week (in which
   * case `ThroughDate < WeekEnd`, which is how a caller knows the week is still in progress).
   * This is deliberately a display value only: the precise cutoff (day index plus local time-of-day)
   * used to truncate baseline weeks for an honest apples-to-apples comparison stays inside the domain
   * and is not exposed — no consumer needs the minute, and exposing it would invite the frontend to
   * reimplement window math.
   */
  throughDate: string;
  timezone: string;
  totals: MetricComparisonResponse;
  weekEnd: string;
  weekStart: string;
}
